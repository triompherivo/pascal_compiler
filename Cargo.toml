[package]
name = "pascal_compiler"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
 # inkwell = { version = "0.1.0", features = ["llvm15-0"] } # Check for the latest version on crates.io